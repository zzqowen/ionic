'use strict'

const ObjectID = require("mongodb").ObjectID;
const globals = require("../globals");
const utils = require("../utils");

exports.update = function(user_id, info){
    updateObj = {
        gender: body.gender,
        country: body.country,
        province: body.province,
        city: body.city,
        brithday: body.brithday
    }

}